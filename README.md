Folder Client:
1. Run command to install dependencies: 
```npm i```
2. Run file index.html

Folder Server:
1. Run command to install dependencies: 
```npm i```
2. Run command to run server:
```npm run start```
